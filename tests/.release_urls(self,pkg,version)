#!/usr/bin/env python
from unittest import main, skip, TestCase
from apipypi import pypi
from assert_is import is_
from dictcls import dictcls
from datetimecls import datetimecls

class TestCase(TestCase):
    #@skip("")
    def test_release_data(self):
        r=pypi.release_urls("osascript", "0.0.1")
        is_(r,list)
        for u in r:
            is_(u,dictcls)

if __name__ == "__main__":
    main()
